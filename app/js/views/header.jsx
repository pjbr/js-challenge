const React = require('react');
const Header = React.createClass({
  render() {
    return (
      <div className= 'header'>
        Contacts Keeper
      </div>
    );
  },
});

module.exports = Header;
